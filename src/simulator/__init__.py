# This file makes the 'simulator' directory a Python package.
# Without it, Python can't import modules from this directory.
# It can be empty, but we use it to define the package version.

__version__ = "1.0.0"
